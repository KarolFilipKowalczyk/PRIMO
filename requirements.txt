# Core (required)
numpy==1.26.4
scipy==1.12.0
networkx==3.2.1
matplotlib==3.8.3
tqdm==4.66.2

# Testing
pytest==8.0.2

# GPU acceleration (optional — install separately with CUDA index)
# pip install torch==2.2.1 --index-url https://download.pytorch.org/whl/cu121
